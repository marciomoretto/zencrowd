<% content_for :title, "Novo Relatório - #{evento_nome_formatado(@evento)}" %>

<div class="d-flex justify-content-between align-items-center page-header mb-3">
  <div>
    <h1 class="page-title mb-1">Novo Relatório</h1>
    <p class="text-muted mb-0"><%= evento_nome_formatado(@evento) %></p>
  </div>
</div>

<div class="card shadow-sm border-0">
  <div class="card-body">
    <%= render 'form' %>
  </div>
</div>
