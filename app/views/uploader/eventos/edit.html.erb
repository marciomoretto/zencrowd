<% content_for :title, "Editar Evento ##{@evento.id}" %>

<div class="page-header">
  <h1 class="page-title">Editar Evento</h1>
  <p class="text-muted mb-0">Atualize os dados e, se desejar, envie uma nova imagem para este evento.</p>
</div>

<div class="card shadow-sm border-0">
  <div class="card-body">
    <%= render 'form', evento: @evento %>
  </div>
</div>
