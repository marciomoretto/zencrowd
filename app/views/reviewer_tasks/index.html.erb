<h1>Tarefas de Revisão</h1>

<% if @images.any? %>
  <table class="table">
    <thead>
      <tr>
        <th>Imagem</th>
        <th>Annotador</th>
        <th>Status</th>
        <th>Ações</th>
      </tr>
    </thead>
    <tbody>
      <% @images.each do |image| %>
        <tr>
          <td><%= image.original_filename %></td>
          <td><%= image.reserver&.name || '-' %></td>
          <td><%= image.status %></td>
          <td>
            <% if image.status == 'submitted' %>
              <%= button_to 'Iniciar Revisão', start_review_image_path(image), method: :post, class: 'btn btn-primary', form: { data: { turbo: false } } %>
            <% elsif image.status == 'in_review' %>
              <%= button_to 'Aprovar', approve_image_path(image), method: :post, class: 'btn btn-success', form: { data: { turbo: false } } %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% else %>
  <p>Nenhuma imagem para revisar.</p>
<% end %>
